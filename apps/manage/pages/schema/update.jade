extends ../../layouts/index

block append title

block append meta

block append stylesheet

block append headjs

block append content
  // Main content
  section.content
    form(role="form",data-module="manage_form",data-collection="schema")
      div
        if data.data && data.data._id
          +manage_ctrl_input({
            name:'_id',
            type:'hidden',
            value:(data.data ? data.data._id + '' : '')
          })
        +manage_ctrl_input({
          label:'数据库名',
          name:'db',
          value:(data.data ? data.data.db : '')
        })
        +manage_ctrl_input({
          label:'表名',
          name:'collection',
          value:(data.data ? data.data.collection : '')
        })
        +manage_ctrl_input({
          label:'表别名',
          name:'alias',
          value:(data.data ? data.data.alias : '')
        })
      +manage_box({
        title:'表定义'
      })
      div
        include ../../../../modules/manage_schema_item/index
      +manage_box({
        title:'字段定义'
      })
      .form-group
        button.btn.btn-primary.btn-schema-add(type="submit") 确定
        | 
        a.btn.btn-default.btn-cancel 取消

block append footjs
  script(type="text/javascript",src="//static.nyouhui.com/dist/js/manage/schema.min.4abe1a11bcfff73b.js")